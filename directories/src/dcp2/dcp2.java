package dcp2;

/**
 * Created by asliakalin on 10/3/18.
 */

/**
 * This problem was asked by Uber.
 *
 * Given an array of integers,
 * return a new array such that each element at index i of the new array
 * is the product of all the numbers in the original array except the one at i.
 *
 * Solve it without using division and in O(n) time.
 * */

public class dcp2 {

    public static int[] productI(int[] nums) {
        int[][] updated = new int[nums.length][nums.length-1];
        for (int i = 0; i < nums.length; i++) {
            return null;
        }
        return null;
    }
}

